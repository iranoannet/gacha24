# Gachamo移行に関する質問事項

## 🔴 優先度：高（移行に必須）

### 1. users.csv
- `rank` の値と意味（例: 0=一般, 1=シルバー, 2=ゴールド など）
- `sub_coin` と `availablePoint` の違いは？両方移行すべき？
- `status` の値（0=退会済み, 1=有効 など）

### 2. pack_cards.csv（※途中で切れています）
- **カラム一覧を再度提供してください**（特に重要：card_id, user_id, stock_status などの有無）
- `stock_status` の値の意味（未開封/開封済み/発送済み/ポイント還元済み など）
- どのカラムで「未発送」「ポイント還元済み」を判別する？

### 3. packs.csv（ガチャマスター）
- `lottery_method` の値と意味（抽選方式の違い？）
- `movie_style` とアニメーションの対応関係
- `payment_method` の値（クレカのみ/ポイントのみ/両方）
- `status` の値（0=非公開, 1=公開, 2=完売 など）

### 4. user_histories.csv（購入履歴）
- `delivery_status` の値の意味（0=未発送, 1=発送済み など）
- `checked_status` の値の意味
- `reduction_point` は何を表す？（使用ポイント？割引？）

### 5. user_history_cards.csv
- `action_type` の値の意味（1=発送, 2=ポイント還元 など？）
- `style` の値の意味
- `status` の値の意味

---

## 🟡 優先度：中（機能復元に必要）

### 6. banners.csv
- `status` の値の意味
- 画像ファイルはどこに保存されている？（ファイルパス）

### 7. admins.csv
- `level` の値と権限の対応（2=管理者, 4=スーパー管理者？）
- 移行対象のadmin一覧（全員移行？特定のみ？）

### 8. coupon_rates.csv / coupon_uses.csv / coupons.csv
- カラム一覧を提供してください（csvが空？）
- クーポン機能は新システムで必要？

### 9. day_datas.csv（日次売上）
- `payment` = 売上、`rieki` = 粗利 で正しい？
- `point` は使用ポイント総額？

### 10. sales_cost_managements.csv
- `wait_arari` の意味は？
- この経理データは移行必要？

---

## 🟢 優先度：低（後から対応可能）

### 11. user_pays.csv / user_points.csv / user_coins.csv
- これらは決済・ポイント付与の履歴？
- `target_shop` は何を指す？（複数店舗運用？）

### 12. transfer_points.csv / user_transfers.csv
- ポイント移行機能は新システムで必要？
- `site_id` / `referral_site_id` の意味

### 13. oversea_waits.csv
- 海外発送の待機リスト？
- `shire_state` の意味

### 14. card_rarities.csv / consts.csv
- カラム一覧を提供してください

---

## 📁 ファイル提供依頼

以下のCSVは**カラムが確認できなかった**ため、再度提供をお願いします：

1. **pack_cards.csv** - 最重要（在庫・発送管理の核心データ）
2. **card_rarities.csv** - レアリティ定義
3. **coupon_rates.csv** - クーポン割引率
4. **coupon_uses.csv** - クーポン使用履歴
5. **coupons.csv** - クーポンマスター
6. **consts.csv** - 定数マスター
7. **pack_card_picks.csv** - 抽選履歴？
8. **post_images.csv / posts.csv** - 投稿機能？
9. **prefectures.csv** - 都道府県マスター

---

## 📊 データ件数確認

各CSVの行数（概算）を教えてください：
- users.csv: 約____件
- pack_cards.csv: 約____件
- user_histories.csv: 約____件
- packs.csv: 約____件

---

## 🎯 移行の優先順位確認

以下の順序で移行予定ですが、変更があればお知らせください：

1. ✅ users.csv → user_migrations → profiles
2. ⏳ userpoint_trigger_histories.csv → inventory_actions (conversion)
3. ⏳ pack_cards.csv → inventory_actions (shipping/pending)
4. ⏳ user_histories.csv → user_transactions
5. ⏳ packs.csv → gacha_masters
6. ⏳ banners.csv → hero_banners
7. ⏳ day_datas.csv → daily_analytics

---

*Generated: 2026-02-05*
*For: Gachamo Legacy System Migration*
